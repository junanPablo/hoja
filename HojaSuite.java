/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package hoja;

import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.runner.RunWith;
import org.junit.runners.Suite;

/**
 *
 * @author Juan Pablo Merck
 */
@RunWith(Suite.class)
@Suite.SuiteClasses({hoja.LSimpleTest.class, hoja.AbListaTest.class, hoja.MiStackTest.class, hoja.FactoryTest.class, hoja.CalculadoraTest.class, hoja.pkg4MainTest.class, hoja.StackListaTest.class, hoja.NodoDobleTest.class, hoja.ListasTest.class, hoja.AbListraTest.class, hoja.LCircularTest.class, hoja.LDobleTest.class, hoja.ICalculadoraTest.class, hoja.StackVectorTest.class, hoja.AbStackTest.class, hoja.StackALTest.class, hoja.NodoTest.class})
public class HojaSuite {

    @BeforeClass
    public static void setUpClass() throws Exception {
    }

    @AfterClass
    public static void tearDownClass() throws Exception {
    }

    @Before
    public void setUp() throws Exception {
    }

    @After
    public void tearDown() throws Exception {
    }
    
}
